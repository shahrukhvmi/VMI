import ComboPackage from "@/components/landingPage/ComboPackage";
import CreativePartner from "@/components/landingPage/CreativePartner";
import DiscountCode from "@/components/landingPage/DiscountCode";
import HolidayUpgrade from "@/components/landingPage/HolidayUpgrade";
import Identity from "@/components/landingPage/Identity";
import LandingGrowBrand from "@/components/landingPage/LandingGrowBrand";
import LandingHeroSection from "@/components/landingPage/LandingHeroSection";
import LandingPackages from "@/components/landingPage/LandingPackages";
import LandingPageFooter from "@/components/landingPage/LandingPageFooter";
import LandingPortfolio from "@/components/landingPage/LandingPortfolio";
import { meta_url } from "@/config/constants";
import MetaLayout from "@/Meta/MetaLayout";
import React, { useRef } from "react";

export default function landingPage() {
  const packagesRef = useRef(null);

  const scrollToPackages = () => {
    packagesRef.current?.scrollIntoView({
      behavior: "smooth",
      block: "start",
    });
  };

  return (
    <>
      <MetaLayout
        title="Essential Website Bundles - Vibrant Media Inc"
        description="Give Your Brand a Holiday Cheer Lift."
        canonical={`${meta_url}essential-website-packages/`}
        data={{
          robots: {
            noindex: true,
            nofollow: true,
          },
        }}
      />
      <main className="relative text-white min-h-screen overflow-hidden landing-main-bg">
        <div className="">
          <LandingHeroSection onPackageClick={scrollToPackages} />
          {/* <LandingGrowBrand /> */}
          <div ref={packagesRef}>
            <LandingPackages isLogoValue={false} />
          </div>
          {/* <DiscountCode /> */}
          <ComboPackage />
          <LandingPortfolio />
          <Identity />
          <CreativePartner />
          <HolidayUpgrade onPackageClick={scrollToPackages} />
          <LandingPageFooter />
        </div>
      </main>
    </>
  );
}

// Disable Layout for this page
landingPage.disableLayout = true;
